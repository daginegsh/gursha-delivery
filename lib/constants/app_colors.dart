import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF5D2E0A);      // Dark Brown
  static const Color primaryDark = Color(0xFF3B1E08);  // Chocolate Brown

  // Accent Colors
  static const Color secondary = Color(0xFFF4B400);    // Golden Yellow
  static const Color secondaryLight = Color(0xFFFFD54F);

  // Background Colors
  static const Color background = Color(0xFFFFF8EE);
  static const Color card = Colors.white;

  // Text Colors
  static const Color textDark = Color(0xFF2C2C2C);
  static const Color textLight = Colors.white;
  static const Color hint = Color(0xFF8A8A8A);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);

  // Border
  static const Color border = Color(0xFFE0E0E0);
}